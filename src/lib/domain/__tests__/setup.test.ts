import { describe, expect, it } from "vitest";

// Placeholder de M0: confirma que el pipeline de tests corre. Los tests
// reales del motor de dinero (Safe-to-Spend, Runway, Poder Adquisitivo
// Real) son obligatorios y entran en M3/M7 — ver PROJECT.md.
describe("test pipeline", () => {
  it("corre", () => {
    expect(true).toBe(true);
  });
});
